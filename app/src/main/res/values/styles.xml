<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

<!--  Activity transitions      -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">false</item>
        <item name="android:windowAllowReturnTransitionOverlap">false</item>
        <item name="android:windowEnterTransition">@transition/fade_in_out</item>
        <item name="android:windowExitTransition">@transition/fade_in_out</item>
        <item name="android:windowReenterTransition">@transition/fade_in_out</item>
        <item name="android:windowReturnTransition">@transition/fade_in_out</item>

<!--  Fragment Shared Element transitions -->
        <item name="android:fragmentSharedElementEnterTransition">@transition/fade_in_out</item>
        <item name="android:fragmentSharedElementReturnTransition">@transition/fade_in_out</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">#000000</item>
        <item name="android:textColorSecondary">#000000</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
    </style>

</resources>


